// CameraRig is superseded — camera is now managed inside ModelObject (RobotModel).
// Kept as an empty stub so any stale imports don't break the build.
export default function CameraRig() {
  return null;
}
